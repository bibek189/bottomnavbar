import 'package:flutter/material.dart';
class Texttt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text("THISISISISSis")),
    );
  }
}